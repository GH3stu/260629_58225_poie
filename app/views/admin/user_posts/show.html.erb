<h1>ユーザー投稿詳細（管理者専用）</h1>

<p>ID：<%= @post.id %></p>
<p>タイトル：<%= @post.title %></p>
<p>本文：<%= simple_format(h(@post.body)) %></p>
<p>投稿者：<%= @post.user.name %></p>
<p>日時：<%= @post.created_at.strftime("%Y-%m-%d %H:%M:%S") %></p>

<hr>

<%= link_to "ユーザー投稿一覧へ戻る", admin_user_posts_path %>
<%= link_to "管理者トップへ戻る", admin_root_path %>
