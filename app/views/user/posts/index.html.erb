<h1>投稿一覧</h1>

<% @posts.each do |post| %>
  <div>
    <h3><%= link_to post.title, user_post_path(post) %></h3>

    <p><%= simple_format(h(post.body).truncate(50)) %></p>

    <p>投稿者：<%= post.user.name %></p>
    <p>投稿日時：<%= post.created_at.strftime("%Y-%m-%d %H:%M:%S") %></p>
  </div>
<% end %>
