<h5>poieへようこそ!</h5>

<br>

<h3>目的から探す</h3>

<br>

<div style="display: flex; justify-content: center; gap: 24px; margin-top: 20px;">
  <% @purposes.each do |purpose| %>
    <div style="border: 0px solid #CCFFCC; background-color: #F7FBEF; padding: 28px; width: 120px; text-align: center;">
      <%= link_to purpose.name, purpose_posts_path(purpose.id), style: "text-decoration: none; font-size: 16px;" %>
    </div>
  <% end %>
</div>

<br><br>

<h5>最新の投稿</h5>

<br>

<% @admin_posts.each do |post| %>
  <div style="border:1px solid #ccc; padding:10px; margin-bottom:10px;">

    <div style="
      font-size:18px;
      font-weight:bold;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    ">

      <!-- 日時（リンク化しない） -->
      <span style="color:#666; font-size:14px;">
        <%= post.created_at.strftime("%Y-%m-%d %H:%M") %>
      </span>

      <!-- 題名（リンク化する：ログイン状態で分岐） -->
      <% if current_user.present? && post.user_id.present? %>
        <%= link_to post.title, user_post_path(post),
          style: "margin-left: 8px; color:#000; text-decoration:none;" %>
      <% else %>
        <%= link_to post.title, admin_post_path(post),
          style: "margin-left: 8px; color:#000; text-decoration:none;" %>
      <% end %>

    </div>

  </div>
<% end %>

<%= paginate @admin_posts %>
